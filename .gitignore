# Build-time SheetJS (fetched via `npm pack xlsx@0.18.5`, inlined into index.html)
package/
xlsx-*.tgz

# Large / transient data artifacts (regenerated by the seeders + workflows).
# The app ships with NO seed wines — these seeder outputs are analysis inputs only,
# never inlined into index.html (the wine side is filled by real producer uploads).
seed_se.json
seed_fi.json
assortment.json
alko_*.xlsx
alko_*.tsv
alko_*.csv

# Generated analysis reports (regenerate with the scripts)
gap_analysis.md
gap_analysis.json

# Local scratch
*.pyc
__pycache__/
