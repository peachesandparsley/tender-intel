# Build-time SheetJS (fetched via `npm pack xlsx@0.18.5`, inlined into index.html)
package/
xlsx-*.tgz

# Large / transient data artifacts (regenerated by the seeders + workflows).
# The app inlines seed_sample.json (committed); the full sets are not committed.
seed_se.json
seed_fi.json
assortment.json
alko_*.xlsx
alko_*.tsv
alko_*.csv

# Generated analysis reports (regenerate with the scripts)
gap_analysis.md
gap_analysis.json

# Local scratch
*.pyc
__pycache__/
